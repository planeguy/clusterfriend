define([]
    , function () {
        return function Feed(user) {
            var me = this;

            function list(nextafter) {
                //get X posts, count(relatedpost) 
                //  WHERE poster is a friend of user
                //  ORDER BY post.datetime desc
            }
        };
    });